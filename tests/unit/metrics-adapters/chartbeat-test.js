import { moduleFor, test } from 'ember-qunit';

moduleFor('metrics-adapter:chartbeat', 'chartbeat adapter', {
});

test('it exists', function(assert) {
  let adapter = this.subject();
  assert.ok(adapter);
});
