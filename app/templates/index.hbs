<h2>Gothamist Homepage</h2>

{{#each model as |story|}}
  <article>
    <h3>
      {{link-to story.title 'story' story.permalink}}
    </h3>
    <p>{{story.authorNickname}}</p>

    {{{story.excerptPretty}}}
  </article>
{{/each}}
