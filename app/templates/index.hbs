<h2>Gothamist Homepage</h2>

{{#each model as |story|}}
  <article>
    <h3>
      {{link-to story.title 'wrapper' story.permalink}}
    </h3>
    <p>{{story.author_nickname}}</p>

    {{!-- template-lint-disable no-triple-curlies --}}
    {{{story.excerpt_pretty}}}
  </article>
{{/each}}
